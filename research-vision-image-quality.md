# OpenAI Vision API 图片质量要求研究

## 研究日期
2025-12-24

## 核心发现

### 1. 图片格式和大小限制

**支持的文件格式：**
- PNG (.png)
- JPEG (.jpeg, .jpg)
- WEBP (.webp)
- 非动画 GIF (.gif)

**大小限制：**
- 单个请求最大 50 MB 总负载
- 单个请求最多 500 张图片
- Base64 编码推荐最大 20MB

### 2. Detail 参数详解

OpenAI Vision API 提供三种图片处理模式：

#### `detail: "low"` (低分辨率模式)
- 模型接收 **512px x 512px** 的低分辨率版本
- 消耗 **85 tokens**
- 适用场景：识别主要形状、颜色等不需要高分辨率细节的任务
- 优点：速度快，成本低

#### `detail: "high"` (高分辨率模式)
- 图片首先缩放到 **2048px x 2048px** 正方形内（保持宽高比）
- 然后分割成多个 **768px** 的瓦片进行详细分析
- 适用场景：需要识别细节、文字、复杂图案的任务
- 优点：识别准确度高

#### `detail: "auto"` (自动模式)
- 模型自动决定使用 low 还是 high
- 默认值（如果不指定参数）

### 3. 图片相似度对比的最佳实践

根据文档和社区讨论，对于图片相似度对比任务：

**推荐配置：**
- 使用 `detail: "high"` 模式
- 图片分辨率建议：**1024px - 2048px** 之间（长边）
- 保持原始宽高比，不要强制裁剪
- 确保图片清晰，避免模糊

**原因：**
1. 饰品细节图需要识别款式的细微差异（材质、纹理、图案等）
2. 高分辨率模式能更准确地捕捉这些细节
3. 2048px 是 API 的最大处理尺寸，超过会自动缩小

### 4. 成本考虑

**GPT-4.1-mini 图片 token 计算：**
- 基于 32px x 32px 的 patch 数量
- 最大 1536 patches (约 1536 tokens)
- 乘以模型倍数（gpt-4.1-mini: 1.62）

**示例成本：**
- 1024 x 1024 图片 = 1024 tokens × 1.62 = **1659 tokens**
- 1800 x 2400 图片 = 1452 tokens × 1.62 = **2352 tokens**

对于相似度对比，每次需要：
- 1 张新图片
- N 张数据库图片
- 总成本 = (1 + N) × 图片 tokens × API 价格

### 5. 图片质量优化建议

**拍摄环境：**
- 充足自然光（5300K 色温）
- 避免阴影和反光
- 纯色背景（白色或深色）

**图片处理：**
- 保存为 JPEG 格式（质量 85-95%）
- 分辨率：1024px - 2048px（长边）
- 文件大小：控制在 2-5 MB
- 保持原始宽高比

**避免的问题：**
- 模糊、过曝、欠曝
- 旋转或倒置
- 水印或 logo
- 过度压缩导致失真

## 针对饰品入库应用的建议

### 细节图拍摄阶段
1. 显示拍照提示：
   - "请在充足自然光（日光）下拍摄"
   - "保持相机稳定，避免模糊"
   - "确保饰品占据画面主体"

2. 图片保存：
   - 格式：JPEG
   - 质量：90%
   - 目标分辨率：1536px（长边）
   - 保持原始宽高比

### AI 相似度对比
1. 使用 `detail: "high"` 模式
2. 相似度阈值：90%（可根据实际效果调整）
3. 批量对比优化：
   - 如果数据库产品数量 > 50，考虑先用图片特征向量预筛选
   - 只对预筛选后的候选产品进行详细 AI 对比

### 成本估算
假设数据库有 100 个产品：
- 每次查重需要对比 101 张图片（1 新 + 100 旧）
- 每张图片约 1659 tokens（1024x1024）
- 总 tokens: 101 × 1659 = 167,559 tokens
- 成本（GPT-4.1-mini）: 约 $0.08 per 查重

**优化方案：**
- 实现图片缓存，避免重复上传
- 使用向量数据库（如 FAISS）进行初步筛选
- 只对相似度高的候选进行 AI 精确对比

## 参考资料
- OpenAI Vision API 官方文档: https://platform.openai.com/docs/guides/images-vision
- OpenAI 社区讨论: https://community.openai.com/t/using-the-vision-api-best-practices/942342
