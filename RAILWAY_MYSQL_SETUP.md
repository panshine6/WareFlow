# Railway MySQL 数据库配置指南

## 📋 操作步骤

### 第一步：添加 MySQL 服务

1. **打开您的 Railway 项目**
   - 访问：https://railway.com/project/3709d3b6-24a6-408a-91cf-759cff5a9898
   - 确保您已登录

2. **添加 MySQL 数据库**
   - 点击右上角的 **"+ New"** 按钮
   - 选择 **"Database"**
   - 选择 **"Add MySQL"**
   - 等待 2-3 分钟，数据库创建完成

3. **获取数据库连接信息**
   - 点击新创建的 **"MySQL"** 服务卡片
   - 点击 **"Variables"** 标签
   - 找到 **`DATABASE_URL`** 变量
   - 点击复制按钮（📋）复制完整的连接字符串

---

### 第二步：更新 Web 服务的环境变量

1. **返回项目主页**
   - 点击左上角的项目名称 **"perceptive-integrity"**

2. **进入 Web 服务设置**
   - 点击 **"web"** 服务卡片
   - 点击 **"Variables"** 标签

3. **更新 DATABASE_URL**
   - 找到现有的 **`DATABASE_URL`** 变量
   - 点击编辑按钮（✏️）
   - 粘贴刚才复制的新 MySQL 连接字符串
   - 点击 **"Update"** 保存

4. **触发重新部署**
   - 保存变量后，Railway 会自动提示重新部署
   - 点击 **"Deploy"** 按钮
   - 或者点击 **"Deployments"** 标签，然后点击 **"Redeploy"**

---

### 第三步：等待部署完成

1. **查看部署状态**
   - 在 **"Deployments"** 标签中查看最新部署
   - 等待状态变为 **"SUCCESS"**（绿色）
   - 预计时间：**3-5 分钟**

2. **查看部署日志**
   - 点击最新的部署记录
   - 点击 **"View Logs"**
   - 确认看到类似以下成功信息：
     ```
     ✓ Database migration completed
     ✓ Server started on port 3000
     ```

---

### 第四步：验证数据库连接

1. **测试 API 健康检查**
   
   在浏览器中打开：
   ```
   https://web-production-e22eb.up.railway.app/api/health
   ```
   
   应该看到：
   ```json
   {"ok":true,"timestamp":...}
   ```

2. **测试数据库查询**
   
   在浏览器中打开：
   ```
   https://web-production-e22eb.up.railway.app/api/trpc/sync.status
   ```
   
   应该看到正常的 JSON 响应（不再是错误信息）

---

## 🔍 常见问题

### Q1: 找不到 "+ New" 按钮？

**解决方案**：
- 确保您在项目主页（不是在某个服务的详情页）
- 按钮通常在页面右上角或项目画布的空白区域

### Q2: MySQL 创建失败？

**解决方案**：
- 检查 Railway 账号是否有足够的配额
- 免费套餐限制：$5/月
- 如果超出限制，需要升级到 Pro 套餐

### Q3: 部署后仍然显示数据库错误？

**解决方案**：
1. 检查 `DATABASE_URL` 是否正确复制（包含完整的连接字符串）
2. 确保 MySQL 服务状态为 "Active"
3. 查看部署日志中的详细错误信息
4. 尝试手动触发重新部署

### Q4: 如何查看 MySQL 数据库内容？

**解决方案**：
1. 在 Railway 项目中点击 MySQL 服务
2. 点击 "Data" 标签查看表和数据
3. 或使用 MySQL 客户端工具（如 MySQL Workbench）连接

---

## 📊 预期结果

完成以上步骤后，您应该能够：

- ✅ MySQL 数据库正常运行
- ✅ Web 服务成功连接到数据库
- ✅ API 端点返回正常数据
- ✅ 应用可以创建用户、添加产品等操作
- ✅ 数据持久化存储，不会丢失

---

## 🚀 下一步

数据库配置完成后：

1. **在电脑浏览器中测试应用**
   ```
   https://8081-idf2b3074077ybpvbjl3s-089ebb62.us2.manus.computer
   ```

2. **在手机上使用 Expo Go 测试**
   - 扫描之前的二维码
   - 应用应该正常加载，不再显示"乱码"

3. **执行完整的功能测试**
   - 创建管理员账号
   - 添加产品
   - 测试云同步
   - 验证所有功能

---

## 💡 提示

- Railway MySQL 使用 **MySQL 8.0**
- 数据库会自动备份
- 免费套餐包含 **512MB 存储空间**
- 数据库和 Web 服务在同一个 Railway 项目中，内网连接速度快

---

## 📞 需要帮助？

如果遇到任何问题，请：

1. 截图 Railway 的部署日志
2. 截图环境变量配置页面
3. 告诉我具体的错误信息

我会立即协助您解决！
