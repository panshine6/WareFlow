# 店小秘批量上传模版格式研究

## 核心发现

店小秘（Dianxiaomi）是一个跨境电商 ERP 系统，支持多平台产品批量导入导出。根据官方文档，Excel 模板格式具有以下特征：

### 必填字段（带 * 标记）

根据文档说明，店小秘的产品导入模板包含以下关键字段：

1. **Parent SKU**：父 SKU，用于多变种产品合并
2. **SKU**：产品 SKU 编号（唯一标识）
3. **产品标题**：产品名称
4. **库存数量**：可用库存
5. **价格（USD）**：产品价格
6. **图片 URL**：产品图片链接（仅支持 URL 格式）

### 模板结构规则

1. **每个 SKU 一行**：每个产品变种占据一行
2. **变种合并**：多变种产品需要保持 Parent SKU 一致，系统会自动合并
3. **图片格式**：仅支持图片 URL，不支持本地路径
4. **字体一致性**：Excel 表格中所有单元格字体必须一致，否则导入失败

### 支持平台

店小秘支持以下平台的批量导入：
- Wish、速卖通、Shopee、Joom、Shopify、Lazada、TikTok、Ozon、Temu、Shein 等

## 针对本项目的 Excel 导出方案

由于用户需求是**时尚饰品入库**场景，不涉及多平台发布，我们可以简化模板，仅包含核心字段：

### 简化模板字段

| 字段名称 | 说明 | 是否必填 |
|---------|------|---------|
| SKU | 产品 SKU 编号 | 是 |
| 产品图片 | 产品细节照片 URL | 是 |
| 库存数量 | 入库数量 | 是 |
| 存储位置 | 仓库存储位置 | 是 |
| 入库时间 | 入库日期时间 | 否 |

### 技术实现方案

1. **Excel 生成库**：使用 `xlsx` 或 `exceljs` npm 包
2. **图片处理**：将本地图片转换为可访问的 URL（可使用 base64 或上传到云存储）
3. **文件分享**：使用 React Native 的 `expo-sharing` 模块导出文件

### 店小秘兼容性

如果用户后续需要将导出的 Excel 导入店小秘系统，可以在导出时添加以下字段以保持兼容：

- Parent SKU（可与 SKU 相同）
- 产品标题（可设置为 SKU 或自定义）
- 价格（USD）（可选填或留空）

## 参考链接

- 店小秘官方文档：https://help.dianxiaomi.com/article/productManagement/222
- 批量导入视频教程：http://help.dianxiaomi.com/help/getRefreshContent.htm?id=336
