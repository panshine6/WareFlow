# 数据同步功能使用指南

## 📱 功能概述

时尚饰品入库助手现在支持**本地和云端数据同步**功能！您可以：
- ✅ 将本地数据备份到云端
- ✅ 从云端恢复数据到本地
- ✅ 在多台设备之间同步数据

---

## 🎯 使用场景

### 场景 1：数据备份
**问题**：担心手机丢失或损坏导致数据丢失？

**解决方案**：
1. 定期点击 **"⬆️ 上传到云端"** 按钮
2. 数据会安全保存在 Railway 云端数据库
3. 即使手机丢失，数据也不会丢失

### 场景 2：更换设备
**问题**：换了新手机，想把旧手机的数据转移过来？

**解决方案**：
1. 在**旧手机**上点击 **"⬆️ 上传到云端"**
2. 在**新手机**上安装应用并登录
3. 点击 **"⬇️ 从云端下载"**
4. 所有数据自动恢复到新手机

### 场景 3：多设备使用
**问题**：想在多台设备上使用同一份数据？

**解决方案**：
1. 在主设备上点击 **"⬆️ 上传到云端"**
2. 在其他设备上点击 **"⬇️ 从云端下载"**
3. 数据在所有设备上保持一致

---

## 📖 使用步骤

### 1. 打开库存管理页面

在应用底部导航栏点击 **"库存"** 标签。

### 2. 找到数据同步区域

在页面顶部，**"导出 Excel"** 按钮下方，您会看到 **"数据同步"** 区域。

### 3. 查看同步状态

同步区域显示：
- **本地**: 本地存储的产品数量
- **云端**: 云端数据库的产品数量
- **最后同步**: 上次同步的时间
- **⚠️ 警告**: 如果本地和云端数量不一致，会显示警告

### 4. 上传到云端

**操作**：点击 **"⬆️ 上传到云端"** 按钮

**效果**：
- 将本地所有数据上传到云端
- **覆盖云端现有数据**
- 上传成功后显示上传数量

**注意**：
- ⚠️ 上传会覆盖云端数据，请确认操作
- ✅ 需要网络连接
- ✅ 需要登录账号

### 5. 从云端下载

**操作**：点击 **"⬇️ 从云端下载"** 按钮

**效果**：
- 从云端下载所有数据到本地
- **覆盖本地现有数据**
- 下载成功后自动刷新列表

**注意**：
- ⚠️ 下载会覆盖本地数据，系统会弹出确认对话框
- ✅ 需要网络连接
- ✅ 需要登录账号

---

## ⚠️ 重要提示

### 1. 数据覆盖警告

- **上传到云端**：会**删除云端所有数据**，然后上传本地数据
- **从云端下载**：会**删除本地所有数据**，然后下载云端数据

**建议**：
- 上传前确认本地数据是最新的
- 下载前确认云端数据是最新的
- 如有疑问，先导出 Excel 备份

### 2. 网络要求

- 同步功能需要稳定的网络连接
- 如果网络不稳定，可能导致同步失败
- 同步失败不会影响本地数据

### 3. 登录要求

- 同步功能需要登录账号
- 如果未登录，会提示登录
- 使用 Manus OAuth 登录

### 4. 数据库配置

- 同步功能需要配置云端数据库
- 如果未配置数据库，同步区域不会显示
- 数据库配置请参考 `DATABASE_SETUP.md`

---

## 🔧 同步策略

### 简单覆盖策略

当前版本使用**简单覆盖策略**：
- 上传：本地 → 云端（覆盖）
- 下载：云端 → 本地（覆盖）

**优点**：
- ✅ 简单易懂
- ✅ 不会产生冲突
- ✅ 操作可预测

**缺点**：
- ❌ 可能丢失未同步的数据
- ❌ 不支持自动合并

**适用场景**：
- 单用户使用
- 主要在一台设备上使用
- 偶尔切换设备

### 未来改进

计划在未来版本中支持：
- 🔄 **智能合并**：自动合并本地和云端数据
- 📊 **冲突检测**：检测并提示数据冲突
- 🔄 **增量同步**：只同步变更的数据
- ⏰ **自动同步**：应用启动时自动同步

---

## 📊 同步状态说明

### 本地和云端数量一致

```
本地: 50 条 | 云端: 50 条
最后同步: 2025-12-26 20:30
```

**说明**：数据已同步，无需操作

### 本地和云端数量不一致

```
本地: 50 条 | 云端: 48 条
最后同步: 2025-12-26 20:30
⚠️ 本地和云端数据不一致
```

**说明**：
- 本地有新数据未上传，或
- 云端有新数据未下载

**建议操作**：
- 如果本地有新数据 → 点击 **"上传到云端"**
- 如果云端有新数据 → 点击 **"从云端下载"**

### 从未同步

```
本地: 50 条 | 云端: 0 条
从未同步
```

**说明**：首次使用同步功能

**建议操作**：
- 点击 **"上传到云端"** 备份数据

---

## 🛠️ 故障排查

### 问题 1：同步区域不显示

**原因**：未配置云端数据库

**解决方案**：
1. 检查 `.env.local` 文件中是否配置了 `DATABASE_URL`
2. 参考 `DATABASE_SETUP.md` 配置数据库
3. 重启应用

### 问题 2：上传失败

**可能原因**：
- 网络连接不稳定
- 未登录账号
- 数据库连接失败

**解决方案**：
1. 检查网络连接
2. 确认已登录账号
3. 查看错误提示信息
4. 稍后重试

### 问题 3：下载失败

**可能原因**：
- 网络连接不稳定
- 未登录账号
- 数据库连接失败

**解决方案**：
1. 检查网络连接
2. 确认已登录账号
3. 查看错误提示信息
4. 稍后重试

### 问题 4：同步后数据丢失

**原因**：下载操作覆盖了本地数据

**解决方案**：
1. 如果有 Excel 导出文件，可以手动恢复
2. 联系技术支持
3. 未来版本会支持数据恢复功能

**预防措施**：
- 定期导出 Excel 备份
- 下载前确认云端数据是最新的
- 看到确认对话框时仔细阅读

---

## 💡 最佳实践

### 1. 定期备份

**建议**：每周至少上传一次到云端

**操作**：
1. 打开库存管理页面
2. 点击 **"⬆️ 上传到云端"**
3. 确认上传成功

### 2. 换设备前同步

**建议**：换设备前先上传数据

**操作**：
1. 在旧设备上点击 **"⬆️ 上传到云端"**
2. 确认上传成功
3. 在新设备上点击 **"⬇️ 从云端下载"**

### 3. 多设备使用

**建议**：使用前先下载最新数据

**操作**：
1. 打开应用
2. 进入库存管理页面
3. 检查同步状态
4. 如果云端数量更多，点击 **"⬇️ 从云端下载"**

### 4. 结合 Excel 导出

**建议**：重要数据同时导出 Excel

**操作**：
1. 点击 **"📊 导出 Excel"**
2. 保存 Excel 文件到云盘（如 iCloud、Google Drive）
3. 再点击 **"⬆️ 上传到云端"**

---

## 🔐 数据安全

### 1. 数据加密

- 数据传输使用 HTTPS 加密
- 数据库连接使用 SSL/TLS
- 密码不会明文存储

### 2. 访问控制

- 需要登录才能同步
- 每个用户只能访问自己的数据
- 使用 OAuth 认证

### 3. 数据备份

- Railway 自动备份数据库
- 可以手动创建备份
- 支持数据恢复

### 4. 隐私保护

- 数据只在您的设备和云端之间传输
- 不会分享给第三方
- 符合隐私保护法规

---

## 📞 技术支持

如果您在使用同步功能时遇到问题，请：

1. **查看文档**：
   - `DATABASE_SETUP.md` - 数据库配置
   - `HOW_TO_REPORT_BUGS_AND_CONTINUE_DEVELOPMENT.md` - 问题反馈

2. **联系支持**：
   - 在 Manus 开启新对话
   - 提供 GitHub 仓库链接
   - 描述具体问题

3. **提供信息**：
   - 错误信息截图
   - 同步状态截图
   - 设备和系统版本

---

## 🎉 总结

数据同步功能让您的数据更安全、更灵活：
- ✅ 定期上传备份数据
- ✅ 换设备时轻松迁移
- ✅ 多设备协同使用
- ✅ 结合 Excel 导出双重保障

记住：
1. 📤 **上传到云端** = 本地覆盖云端
2. 📥 **从云端下载** = 云端覆盖本地
3. ⚠️ 下载前会弹出确认对话框
4. 💾 重要数据记得导出 Excel

祝您使用愉快！🚀

---

**文档版本**: v1.0
**最后更新**: 2025-12-26
**作者**: Manus AI Assistant
