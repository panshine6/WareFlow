# 时尚饰品入库助手 - 使用说明

## 应用简介

时尚饰品入库助手是一款专为仓库管理员设计的移动应用，帮助您快速完成时尚饰品的入库管理工作。应用支持拍照录入、AI 智能识别产品数量、本地数据存储和导出店小秘批量上传模板。

## 核心功能

### 1. 产品入库流程

**步骤一：拍摄产品细节照片**
- 点击主屏幕的「添加产品」按钮
- 授予相机权限
- 使用手机相机拍摄产品细节照片
- 建议使用微距模式（如设备支持）以获得清晰的产品照片

**步骤二：输入 SKU 编号**
- 输入产品的 SKU 编号（例如：ACC-2024-001）
- 应用会自动记住上次输入的 SKU，方便批量入库同类产品
- 点击「确定」进入下一步

**步骤三：拍摄全景照片并识别数量**
- 将所有产品摊开放在深色背景上
- 确保每个产品的白色标签（约 3cm × 5cm）清晰可见
- 拍照后，应用会自动使用 AI 识别产品数量
- 您可以手动修改识别结果，确保准确性
- 点击「确认」保存数量

**步骤四：输入存储位置**
- 输入产品的存储位置（例如：A区-01-03）
- 应用会记住默认存储位置，下次自动填充
- 点击「完成」保存入库记录

### 2. 库存管理

**查看库存列表**
- 切换到「库存」标签页
- 查看所有已入库的产品记录
- 每条记录显示：产品照片、SKU、数量、存储位置

**搜索产品**
- 在搜索框中输入 SKU 关键词
- 实时过滤匹配的产品记录

**导出数据**
- 点击「导出 Excel」按钮
- 应用会生成店小秘批量上传格式的 Excel 文件
- 通过系统分享功能保存或发送文件

### 3. 主屏幕统计

主屏幕显示两个关键统计数据：
- **今日入库**：当天新增的产品记录数量
- **总库存**：所有产品的总数量

## AI 识别功能说明

### 当前实现

应用集成了 OpenAI Vision API 用于智能识别产品数量。由于需要 API 密钥，当前版本使用**模拟数据**进行演示。

### 启用真实 AI 识别

如需启用真实的 AI 识别功能，请按以下步骤操作：

1. 获取 OpenAI API 密钥
   - 访问 https://platform.openai.com/
   - 注册账号并创建 API 密钥

2. 修改代码
   - 打开文件：`app/add-product-overview.tsx`
   - 找到第 51-58 行的代码
   - 将模拟数据替换为真实 API 调用：

```typescript
// 替换前（模拟数据）
const mockCount = Math.floor(Math.random() * 15) + 1;
setQuantity(mockCount);

// 替换后（真实 API）
const apiKey = "your-openai-api-key"; // 替换为您的 API 密钥
const count = await countProductsInImage(base64, apiKey);
setQuantity(count);
```

3. 重新部署应用

### AI 识别提示

为获得最佳识别效果：
- 将产品摊开放置，避免重叠
- 使用深色背景（黑色或深蓝色）
- 确保光线充足
- 白色标签清晰可见
- 相机与产品保持垂直角度

## 数据存储

所有数据存储在设备本地，使用 AsyncStorage 技术：
- **优点**：无需网络连接，数据完全私密
- **注意**：卸载应用会清空所有数据，请定期导出备份

## Excel 导出格式

导出的 Excel 文件包含以下字段，兼容店小秘批量上传模板：

| 字段名 | 说明 |
|--------|------|
| Parent SKU | 父级 SKU |
| SKU | 产品 SKU 编号 |
| 产品标题 | 自动生成（格式：时尚饰品-{SKU}） |
| 产品图片 | 产品细节照片路径 |
| 库存数量 | AI 识别或手动输入的数量 |
| 价格(USD) | 留空，需手动填写 |
| 存储位置 | 仓库存储位置 |
| 入库时间 | 记录创建时间 |

## 常见问题

**Q: 相机无法打开？**  
A: 请检查应用权限设置，确保已授予相机权限。

**Q: AI 识别不准确怎么办？**  
A: 可以在数量确认界面手动修改识别结果，或重新拍照。

**Q: 如何批量入库同一 SKU 的产品？**  
A: 应用会记住上次输入的 SKU 和存储位置，连续入库时无需重复输入。

**Q: 导出的 Excel 文件在哪里？**  
A: 导出后会弹出系统分享界面，您可以选择保存到文件、发送邮件或分享到其他应用。

**Q: 数据会同步到云端吗？**  
A: 不会。所有数据仅存储在本地设备，确保隐私安全。

## 技术支持

如有任何问题或建议，请联系开发团队。

---

**版本**：1.0.0  
**更新日期**：2024年12月23日
