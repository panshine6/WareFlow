[phases.setup]
nixPkgs = ['nodejs_22', 'pnpm']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

# Build phase removed - no build needed for backend API

[start]
cmd = 'pnpm run railway:start'
