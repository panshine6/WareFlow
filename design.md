# 时尚饰品入库助手 - 界面设计文档

## 设计理念

本应用专为仓库管理员设计，采用简洁高效的 iOS 风格界面，遵循 Apple Human Interface Guidelines。所有界面均针对**移动竖屏（9:16）**和**单手操作**优化。

## 配色方案

- **主色调**：#007AFF（iOS 蓝色）- 用于主要操作按钮和强调元素
- **背景色**：#FFFFFF（浅色模式）/ #000000（深色模式）
- **文本层级**：
  - 主文本：#11181C（浅色）/ #ECEDEE（深色）
  - 次要文本：#687076（浅色）/ #9BA1A6（深色）
  - 禁用文本：#C0C0C0
- **功能色**：
  - 成功：#34C759（绿色）
  - 警告：#FF9500（橙色）
  - 错误：#FF3B30（红色）

## 屏幕列表

### 1. 主屏幕（Home）
**主要内容**：
- 顶部：应用标题和统计卡片（今日入库数量、总库存）
- 中部：大型「添加产品」按钮（主要操作入口）
- 底部：最近入库记录列表（显示缩略图、SKU、数量、时间）

**功能**：
- 点击「添加产品」按钮 → 进入拍照流程
- 点击记录条目 → 查看产品详情
- 下拉刷新列表

### 2. 拍照流程（多步骤）

#### 步骤 1：产品微距拍照
- 全屏相机界面，自动启用微距模式
- 顶部：提示文字「请拍摄产品细节照片」
- 底部：拍照按钮（大圆形）、取消按钮
- 拍照后自动保存并进入下一步

#### 步骤 2：SKU 录入
- 半屏底部弹出表单
- 输入框：SKU（带自动填充上次输入值）
- 按钮：「确定」「取消」
- 确定后进入下一步

#### 步骤 3：产品全景拍照
- 全屏相机界面
- 顶部：提示文字「请将产品摊开放在深色背景上拍摄全景」
- 底部：拍照按钮、重拍按钮
- 拍照后调用 AI 识别

#### 步骤 4：数量确认
- 半屏底部弹出卡片
- 显示：AI 识别的数量（大字号）
- 可编辑输入框：允许手动修改数量
- 按钮：「确认」「重拍」

#### 步骤 5：存储位置
- 半屏底部弹出表单
- 输入框：存储位置（带默认值，记住上次输入）
- 按钮：「完成」「取消」
- 完成后保存记录并返回主屏幕

### 3. 产品详情屏幕
**主要内容**：
- 顶部：产品细节照片（大图）
- 中部：信息卡片
  - SKU
  - 库存数量
  - 存储位置
  - 入库时间
- 底部：产品全景照片
- 导航栏：返回按钮、编辑按钮、删除按钮

### 4. 库存列表屏幕（Tab 2）
**主要内容**：
- 顶部：搜索框（按 SKU 搜索）
- 列表：所有产品记录
  - 每行显示：缩略图、SKU、数量、位置
- 底部：导出按钮（导出为 Excel）

**功能**：
- 点击记录 → 查看详情
- 点击导出 → 生成店小秘格式 Excel

## 关键用户流程

### 主流程：添加产品入库
1. 用户在主屏幕点击「添加产品」
2. 系统打开相机（微距模式）
3. 用户拍摄产品细节照片 → 自动保存
4. 系统弹出 SKU 输入框（自动填充上次 SKU）
5. 用户输入/确认 SKU → 点击「确定」
6. 系统提示用户摊开产品拍摄全景
7. 用户拍摄全景照片 → AI 识别数量
8. 系统显示识别数量 → 用户确认或修改
9. 系统弹出存储位置输入框（默认上次位置）
10. 用户确认位置 → 点击「完成」
11. 系统保存记录 → 返回主屏幕并刷新列表

### 辅助流程：查看和导出
1. 用户切换到「库存」Tab
2. 浏览或搜索产品记录
3. 点击「导出」按钮
4. 系统生成 Excel 文件并分享

## 布局规范

- **间距**：统一使用 8pt 网格（8、16、24、32）
- **圆角**：
  - 按钮：12pt
  - 卡片：16pt
  - 底部弹出表单：24pt（顶部圆角）
- **触摸目标**：最小 44pt × 44pt
- **安全区域**：所有内容尊重设备安全区域（刘海、底部指示器）
- **主操作区域**：重要按钮放置在屏幕下方 1/3 区域（拇指区）

## 图标使用

- Tab 图标：24-28pt，使用 Material Icons（Android/Web）或 SF Symbols（iOS）
- 按钮图标：20-24pt
- 统一使用填充风格（filled）

## 数据结构

### Product 产品记录
```typescript
interface Product {
  id: string;                    // 唯一标识
  detailImageUri: string;        // 细节照片 URI
  overviewImageUri: string;      // 全景照片 URI
  sku: string;                   // SKU 编号
  quantity: number;              // 库存数量
  storageLocation: string;       // 存储位置
  createdAt: Date;               // 入库时间
}
```

### AppSettings 应用设置
```typescript
interface AppSettings {
  lastSku: string;               // 上次输入的 SKU
  defaultLocation: string;       // 默认存储位置
}
```
